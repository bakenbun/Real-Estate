{
  "name": "buildledger",
  "private": true,
  "version": "1.0.0",
  "description": "Private construction cost ledger with a server-side Supabase boundary.",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "check": "node --check server.js && node --check app.js"
  }
}
